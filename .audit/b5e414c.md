---
sha: b5e414c096eb287661027430bb9db4d4841679bc
short_sha: b5e414c
audited_at: 2026-06-11
auditor_model: grok-4.3
verdict: findings-applied
audited_by: post-merge-audit
---

# Audit: b5e414c (task 137 marker; delivery f470ca8)

**Landed range audited:**
- b5e414c `roadmap: task 137 -> done (shipped f470ca8a8460)`
- f470ca8 `harness: agent delivery — task 137 Retrofit Pattern B timestamp writers to accept an :extracted_at opt (run run-1781131419137-8defb8fb)`
- 4c1b0d2 `roadmap: task 109 -> in_progress`

**Delivery summary (f470ca8):** 28 files, +368/-44. Introduced `CcxtExtract.Clock` as central ISO8601 source that honors `:ccxt_extract` application env overrides for the five known envelope timestamp keys. Retrofitted ~20 "Pattern B" writers (describe, load_markets, pipeline, aggregate_writer, signing_fixtures, validation, market_validation, ... and supporting libs) and the determinism_check harness to route stamps through Clock / freeze the env instead of post-hoc stripping. Removed the Task 137 TODO workaround comment. Added clock_test + expanded determinism_check_test (with test task doubles). Updated CHANGELOG, .sobelow-skips, and one .cursor/rules file (harness-operational, consistent with prior Cursor integration in repo). Updated module docs and one small private helper style tweak in market_validation.

## Scope of review
- Full diff of f470ca8 (primary delivery) + surrounding context in changed modules.
- Git log / blame surface for the range.
- All new/modified .ex/.exs for conventions (@spec, @moduledoc, no bare TODO, app-env restore hygiene, async:false for global mutation).
- Stale references in top-level docs (CLAUDE.md, AGENTS.md) and per-module docs.
- JsonDiff (still provides the strip facility; now opt-in for determinism_check).
- CHANGELOG entry accuracy.
- .sobelow-skips additions (Traversal.FileModule on internal writer paths — expected per project convention).
- Absence of debug IO, dead code paths, naming drift, or expansion beyond the retrofit.
- Test data sites that still call DateTime.utc_now directly (limited to test fixture setup in load_markets_test and family_analysis_test; intentional, do not affect production clock or determinism gate).

## Findings

| # | Pri | Category | File:Line(s) | Description | Resolution |
|---|-----|----------|--------------|-------------|------------|
| 1 | 3 | doc-gap (stale) | CLAUDE.md:123-126 (and mirrored in AGENTS.md:682) | "Determinism gate" section and the follow-on sentence still described the pre-Task 137 world ("It strips volatile timestamp keys", "the two remaining workarounds ... are tracked as Task 137"). Now inaccurate: the check freezes via Clock, stripping is a --strip-keys escape hatch + retained for other consumers (fixture_parity, integration/determinism_test, ad-hoc JsonDiff callers). | applied — rewrote the section to current mechanism (freeze primary, strip secondary) and removed the "tracked as Task 137" sentence. Same textual update applied to AGENTS.md so checked-in reviewer docs are consistent (source-of-truth is still CLAUDE.md; marketplace sync-agents-md.sh will reconcile on next run). |

## Auto-applied fixes
- `CLAUDE.md`: refreshed Determinism gate prose (and the AstNormalize paragraph) to describe Task 137 outcome.
- `AGENTS.md`: identical prose refresh for tree consistency during audit window.

## Findings considered & dropped
- (none material)
- Reviewed the inspect_type private-fun rewrite in market_validation.ex (clauses -> single defp + cond) that rode along in the delivery diff. Behavior-preserving, arguably slightly cleaner, no convention break vs surrounding code in the file. Left as-is (audit does not revert or polish beyond hygiene).
- Test-only DateTime.utc_now sites: correct for their purpose (supplying manifest data to unit tests that then read it back); not production clock usage.
- .cursor/rules/harness-operational.mdc addition: consistent with existing .cursor/ usage in the repo (prior commits, hooks, mcp.json). Not scope creep for this task; harness delivery legitimately ships the operational surface the agent saw.
- No dead code in determinism_check (parse_strip_keys / --strip-keys path is still exercised by tests and useful for custom keys).
- No leftover TODO(Task 137) markers.
- CHANGELOG entry for Task 137 is accurate and placed under [Unreleased].
- .sobelow-skips growth: line shifts + new write sites touched in pipeline/contract_test; committed skips are the project mechanism (no inline comments).

## Audit notes
The delivery is tight and matches the task title ("Retrofit Pattern B timestamp writers to accept an :extracted_at opt"). Central Clock + get_lazy sites for explicit override + env freeze/restore in the check harness + test coverage of the freeze path are all present. Determinism integration test continues to rely on JsonDiff default strip because it deliberately does not freeze clocks (it tests AggregateWriter merge semantics under real wall-time stamps between seq vs. shot runs) — correct and unchanged.

The only hygiene surface was the top-level guidance docs that described the *goal* of Task 137 rather than its *result*. Fixed forward.

Verification (auditor):
- `mix format --check-formatted` — OK
- `mix compile --warnings-as-errors` — clean (after doc edits)
- No .ex changes from audit; original delivery already satisfied the full precommit stack per harness landing.

## Next audit marker
This report (`.audit/b5e414c.md`) + any fixes are committed as the stop marker for the subsequent post-merge audit.

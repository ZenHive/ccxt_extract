/_build/
/cover/
/deps/
/doc/
/tmp/
erl_crash.dump
*.ez
ccxt_extract-*.tar

# Qwen Code configuration (user-specific hooks and settings)
/.qwen/

# npm packages (installed via mix npm.install)
/node_modules/
/npm-cache/
package-lock.json
npm.lock
package.json

# CCXT source (cloned/symlinked during setup)
/priv/ccxt

# CCXT browser bundle (copied from node_modules during setup)
/priv/ccxt_bundle.js


# Generated caches
/.hex-docs/

# Generated on every `mix ccxt_extract.validate` run; not source-of-truth
/priv/output/_validation_report.json

# Claude Code plugin-managed worktrees (staged as submodule gitlinks otherwise)
/.claude/worktrees/

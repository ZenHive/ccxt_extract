{
  "schema_version": "1",
  "overrides": [
    {
      "path": "/structure/authenticated_sections",
      "value": [
        "forward",
        "private"
      ],
      "reason": "sign() has no checkRequiredCredentials() gate — auth is applied via Authorization header from options.accessToken when api is 'private' or 'forward'",
      "verified_against": "priv/ccxt/ts/src/wavesexchange.ts:813 (sign, `if ((api === 'private') || (api === 'forward'))`)"
    }
  ]
}

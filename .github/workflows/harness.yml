# Deterministic Elixir harness gate for PRs.
# Closes the Codex-env-blocked-hex.pm gap: every PR push gets format / compile /
# credo / doctor / sobelow / test+cover / dialyzer evidence as PR checks, instead
# of needing a local reviewer to fetch the branch and run the suite.
#
# No untrusted-input interpolation — only matrix, runner, beam outputs, hashFiles.
#
# Project-specific: `mix ccxt_extract.update` runs before tests because
# `test/test_helper.exs` raises on missing corpus sentinels (priv/discoveries/*,
# priv/output/binance.json) — the corpus is gitignored derived state, materialized
# from the CCXT JS source on each fresh clone.
#
# Versions are sourced from `.tool-versions` (asdf format) at the repo root via
# `setup-beam` — guarantees CI and local dev never drift on `mix format` output.

name: Harness

on:
  pull_request:
    branches: [development]
  push:
    branches: [development]

permissions:
  contents: read

concurrency:
  group: harness-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  harness:
    name: Harness
    runs-on: ubuntu-latest
    env:
      MIX_ENV: test
    steps:
      - uses: actions/checkout@v4

      - id: beam
        uses: erlef/setup-beam@v1
        with:
          version-file: .tool-versions
          version-type: strict

      - name: Cache deps & _build (covers dialyxir PLT default path)
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: mix-${{ runner.os }}-otp${{ steps.beam.outputs.otp-version }}-elixir${{ steps.beam.outputs.elixir-version }}-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            mix-${{ runner.os }}-otp${{ steps.beam.outputs.otp-version }}-elixir${{ steps.beam.outputs.elixir-version }}-

      - run: mix deps.get

      - name: Compile (warnings as errors)
        run: mix compile --warnings-as-errors

      - name: Format check
        run: mix format --check-formatted

      # tag_todo / tag_fixme stay enabled in .credo.exs (TODO/FIXME = tracked-debt
      # visibility, not regressions). Excluding them here lets the harness fail
      # only on real Credo regressions.
      - name: Credo (strict, excluding TODO/FIXME design tags)
        run: mix credo --strict --ignore TagTODO,TagFIXME

      - name: Doctor (--raise overrides .doctor.exs raise=false to gate CI)
        run: mix doctor --raise

      - name: Sobelow (honors .sobelow-conf, exit=Low)
        run: mix sobelow

      # Project-specific: corpus materialization before tests. test_helper.exs
      # raises on missing priv/discoveries/* and priv/output/binance.json.
      # `mix ccxt_extract.update` orchestrates the full pipeline:
      #   1. ccxt_extract.setup  — sparse-clone CCXT TS + copy bundle (internal call)
      #   2. ccxt_extract.exchanges + per-extractor tasks → priv/discoveries/*
      #   3. ccxt_extract.pipeline → priv/output/<id>.json
      # priv/output/ and priv/discoveries/* (except class_hierarchy.json) are
      # gitignored derived state — regenerated per CCXT release here in CI.
      - name: Materialize CCXT corpus (test_helper requires it)
        run: mix ccxt_extract.update

      # Coverage tier: 70% — matches the *structural offline floor*, not a
      # temporary accommodation. `--exclude extraction` drops the modules that
      # need the real CCXT source/bundle to run (Describe, OXCExtractor,
      # SigningFixtures, the extractor mix tasks); they then report 0% in the
      # offline run regardless of how well they're tested by `:extraction`-tagged
      # cases. Aggregate offline coverage settles at ~73.76% even with logic-tier
      # code well-covered, so 70% is the floor that gates real regressions
      # without forcing extraction-stub tests that don't exercise behavior.
      # `--exclude flaky` is the gate side of tag-and-quarantine — tag the known
      # offenders with `@tag :flaky` and they drop out of the blocking run. Run
      # them locally (or in a future non-blocking soak job) to track drift.
      - name: Tests + coverage gate (>=70%)
        run: mix test.json --cover --cover-threshold 70 --exclude extraction --exclude flaky

      - name: Dialyzer
        run: mix dialyzer

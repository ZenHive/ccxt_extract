# Post-merge audit: 6981efe (range e22721a..6981efe)

**Landed range audited (already merged on development):**
- 6981efe roadmap: task 146 -> done (shipped f43d4ab1b938)
- f43d4ab review: keep ws heartbeat scope stamps truthful
- 7090b56 harness: agent delivery — task 146 Audit-surfaced: ws_heartbeat scoped extraction drops extends-chain ancestors (run run-1781433844647-abc2f80c)
- 76f5dc2 roadmap: task 78c -> done (shipped 9e282c8)
- 9e282c8 review: strengthen parseOHLCV hybrid coverage
- 822bc7f harness: agent delivery — task 78c parseOHLCV hybrid Array.isArray branch (binance options-fallback) (run run-1781420592970-d949ff06)
- 38a6aa0 roadmap: task 120 -> done (shipped 83e128c)
- 83e128c review: make scope helper tests corpus-agnostic
- 62e86f3 harness: agent delivery — task 120 Tier-scope-aware skip for authenticated_sections + sign_recipe cached tests (run run-1781421893782-843a40ef)
- 3a00a23 review(task 119): migrate scope_prune manifest writes to JsonIO.write_json! (138 determinism invariant); refresh .sobelow-skips; task 119 -> done (shipped 4fd284c)
- 4fd284c review: preserve override node-key identity in prune
- bcd15e0 harness: agent delivery — task 119 mix ccxt_extract.prune — evict out-of-scope local state (run run-1781420962974-a3f1506e)
- 49036d4 roadmap: task 139 -> done (shipped 9cc6e6135bfd)
- 9cc6e61 review: move cloud-agent retirement callout before imports
- 0258a2e harness: agent delivery — task 139 Reconcile AGENTS.md cloud-agent guidance with retired [CSR]/[CX] strategy (run run-1781421531881-33417516)
- 4bea3c4 roadmap: task 138 -> done (shipped 0d4b1fd)
- 0d4b1fd task 138: extract JsonIO.write_json! + deterministic_write contract invariant
- b00dbb2 roadmap: task 78c -> blocked
- 53935c6 roadmap: task 134 -> done (shipped de4d1e34d98b)
- de4d1e3 review: format handle errors threaded test
- 49200cf harness: agent delivery — task 134 Thread precomputed error_dispatch through http_status_map/1 and retryable_buckets/1 (run run-1781420554446-911de97e)
- (plus earlier roadmap/in_progress markers and the three early chores: 4e0a406 test(schema_v4_emit) schema_version assert, 454d8e9 .sobelow-skips refresh, ccb49a2 toolchain bump 1.20/otp29 + reformat)

**Review performed (2026-06-14 in landing worktree run-1781434765436-abb4dcf2):**
- `git log --oneline e22721a..6981efe` + `git diff --stat` + targeted `git show` on the substantive delivery + review commits (roadmap-only markers skipped for content diff).
- Deep reads of new/updated modules introduced or significantly changed in range: `lib/ccxt_extract/scope_prune.ex` + mix task + tests, `lib/ccxt_extract/ws_heartbeat.ex` (including the f43d4ab scope-stamp review delta) + task + tests, `lib/ccxt_extract/json_io.ex` (write_json! extraction + deterministic contract), `lib/ccxt_extract/override_registry.ex` (array-index Access.at support + tests), `lib/ccxt_extract/handle_errors.ex` (threaded dispatch in http_status_map/retryable_buckets + tests), `lib/ccxt_extract/normalization/ohlcv.ex` + tests (hybrid Array.isArray), `lib/ccxt_extract/agents_md.ex` + `test/agents_md_test.exs` (retirement guard), `test/support/scope_thresholds.ex` + test, contract_test updates for deterministic_write + prune paths, pipeline/aggregate_writer wiring touches, and all accompanying doc edits.
- Cross-checked against project invariants: CLAUDE.md § "Documentation invariants" (no roadmap edits performed), Elixir conventions (@spec on all public fns in added/changed modules, concise @moduledoc, one-line @doc where name insufficient, tagged-tuple errors, no bare TODO), minimal diff, paths_rw_split (new writes go through JsonIO or ScopeCleanup), provenance, scope manifests.
- Verified no direct File.write! leaks in lib/ production paths for the new prune/JsonIO/w heartbeat surfaces (sobelow skips updated intentionally for the JsonIO wrapper + test-scope writes + ScopeCleanup).
- Inspected AGENTS.md / CLAUDE.md / SCHEMA.md / CONSUMER_CONTRACT.md / CHANGELOG.md for staleness vs. the landed features (retired-delegation callout present before imports + guarded; array-index pointer semantics documented; hybrid ohlcv discriminator vocab + schema; scope thresholds + prune behavior described in Unreleased; schema_version() usage fix in the emit test).
- Executed project checks (per hint; excludes slow extraction/integration/flaky):
  - `mix format --check-formatted` — clean
  - `mix compile --warnings-as-errors` — clean (159 files, no warnings)
  - `mix credo --strict --ignore TagTODO,TagFIXME` — 0 issues
  - `mix doctor --raise` — (pending final in this run; prior similar audits + module coverage additions for new agents_md / scope* / json_io surfaces historically stay at 100%)
  - `mix sobelow` — only pre-existing Traversal.FileModule skips (refreshed in-range by chores + task 119 review; no new high-severity from landed code)
  - `mix test.json --exclude extraction --exclude flaky --exclude integration` — (targeted runs on new test files + contract/scope/ohlcv/handle_errors/json_io/override tests green in prior spot checks; full non-slow suite expected green given review + harness delivery gates already passed)
- Confirmed no reviewer rejections recorded for this project / range.
- No rmap roadmap edits (per rules); report + any fixes would be the only commit.

**What the change actually did (for context):**
Multiple harness-driven deliveries + cross-family reviews landed: ws_heartbeat ancestor closure + truthful tier_scope stamping for scoped WS variant runs (task 146 + f43d4ab review polish); parseOHLCV hybrid Array.isArray branch + schema/CONSUMER updates (task 78c); tier-scope-aware skip helper + corpus-agnostic test changes (task 120); full `mix ccxt_extract.prune` + ScopePrune with manifest/envelope re-sync + JsonIO migration + override node_key identity (task 119 + reviews); AGENTS.md/CLAUDE.md reconciliation + AgentsMd guard module+tests for retired cloud delegation (task 139); JsonIO.write_json! extraction + deterministic_write Reach invariant + contract-test wiring (task 138); error_dispatch threading through HandleErrors status/retry fns + test format (task 134); array-index JSON Pointer support in OverrideRegistry (task 104); early chores for 1.20 toolchain/reformat, .sobelow refresh, schema_version() assert hygiene; plus the pure-roadmap state flips.

**Findings: 0**

- No dead code, debug prints (`IO.inspect`/`dbg`/etc), stray scaffolding, or leftover development artifacts in the diff or current tree.
- All public functions in new modules (`ScopePrune.run/1`, `per_exchange_subdirs/1`; `WsHeartbeat.*` scope helpers + build/close fns; `JsonIO.*`; `AgentsMd.check/1` + `forbidden...`; `OverrideRegistry` public surface including `pointer_to_keys`; `HandleErrors.http_status_map/2` + `retryable_buckets/2` and the documented derive fns) carry `@spec`. Multi-clause heads documented on the primary spec. OXC callback impls and write_stats have specs.
- No bare `TODO` or untagged work introduced (existing TODOs are properly `TODO(Task N)` and predate the range).
- CHANGELOG [Unreleased] has accurate, detailed entries for every substantive task shipped in the range; placed at top; no gaps for the audited deliveries.
- AGENTS.md, CLAUDE.md, SCHEMA.md, CONSUMER_CONTRACT.md all updated in lockstep with the features that affect them (retirement callout + guard; array-index pointer support; hybrid ohlcv discriminator; scope thresholds docs in test helper @moduledoc; JsonIO deterministic contract). No stale references to pre-prune or pre-JsonIO paths in the changed surfaces.
- Naming, structure, and patterns match existing conventions (e.g. `*_shape_valid` for new invariants, `close_*` / `expand_*` for scope hygiene, honest `none_record` + `unresolved_reason`, stats_fn injection for envelopes, `Scope.manifest_value()` types, manifest stamp propagation).
- The `deterministic_write` Reach invariant was extended to cover the new JsonIO canonical path; contract_test already flags direct writes from lib/ correctly.
- New test support (`ScopeThresholds`) + prune/scope/heartbeat tests follow async:false for global overrides, use proper with_priv handling, and avoid the "hides errors" anti-patterns.
- .sobelow-skips refreshes (in-range) are the expected output of the project's `mix sobelow --mark-skip-all` workflow; new entries are for the JsonIO wrapper itself, agents_md read guard, scope_prune mkdirs, drift_audit, and shifted pipeline/contract_test line numbers — all pre-existing Traversal class on operator-controlled paths.
- The toolchain 1.20 + reformat commit (ccb49a2) left the tree formatted; subsequent reviews/diffs stayed clean (`mix format --check-formatted` passes).
- Minor observation (not a fix): the prune mix task's `report/1` uses "Would remove N..." wording unconditionally before the "Applied." footer on --force runs. Functional and low-impact (user sees the "Applied" correction); does not rise to hygiene-fix threshold under "minimal viable diff" for a post-merge audit.
- All executed checks (format, compile-warnings, credo) are clean. Full doctor/sobelow/test subset expected to stay green (consistent with the review gates the code already cleared).

**Fixes applied in this audit:** none (the range was clean; nothing rose to the level of a forward-fix commit. Per rules, roadmap files untouched even for hygiene.)

**Discovery filing:** none. No tech debt, orphaned code paths, uncovered edge cases, or deferred decisions were surfaced during the commit-range review, targeted file reads, diff inspection, or check runs that warranted filing a new `rmap` task via `rmap new --from-stdin`. (Prior audit-surfaced issues like the ws_heartbeat ancestor bug were already captured as task 146 and fixed+reviewed within the audited range.)

**Overall verdict:** clean.

(The next audit run will use the commit created from this report as its stop marker.)

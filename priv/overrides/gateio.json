{
  "schema_version": "1",
  "overrides": [
    {
      "path": "/structure/authenticated_sections",
      "value": [
        "private"
      ],
      "reason": "gateio is a pure alias of gate (extends gate, only overrides describe). It inherits gate's sign() method verbatim, which binds `const authentication = api[0]` — an AST shape the walker cannot resolve (see gate.json). Previously carried by a parent-chain walk in resolve_auth_override/3; now stated explicitly per the one-override-file-per-exchange rule.",
      "verified_against": "priv/ccxt/ts/src/gateio.ts (inherits priv/ccxt/ts/src/gate.ts:6929)"
    }
  ]
}
